<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="robots" content="noindex,nofollow"><link rel="icon" href="{{ asset('favicon.svg') }}" type="image/svg+xml"><title>@yield('title') — ValueMap admin</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="admin-body"><header class="admin-header"><a class="brand" href="{{ route('admin.index') }}"><img class="admin-logo" src="{{ asset('images/brand/valuemap-logo.png') }}" alt="VALUEMAP" width="725" height="130"></a><nav><span class="admin-identity">{{ auth()->user()->name }} · {{ auth()->user()->role_label }}</span><a href="{{ route('admin.index') }}">Content</a>@if(auth()->user()->isMainAdmin())<a href="{{ route('admin.users.index') }}">Users</a>@endif<a href="{{ route('home') }}" target="_blank">View website ↗</a><form method="post" action="{{ route('logout') }}">@csrf<button type="submit">Sign out</button></form></nav></header><main class="admin-main">@if(session('success'))<div class="form-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="form-error">{{ $errors->first() }}</div>@endif @yield('content')</main></body></html>
