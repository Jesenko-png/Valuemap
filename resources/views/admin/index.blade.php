@extends('layouts.admin')
@section('title','Content')
@section('content')
<div class="admin-title"><div><p class="eyebrow">Content management</p><h1>Project content</h1><p>{{ $items->total() }} content items · {{ $messageCount }} contact messages stored</p></div><a class="button" href="{{ route('admin.content.create') }}">Add content +</a></div>
<div class="admin-filters"><a @class(['active'=>!$type]) href="{{ route('admin.index') }}">All</a>@foreach(\App\Models\ContentItem::TYPES as $filter)<a @class(['active'=>$type===$filter]) href="{{ route('admin.index',['type'=>$filter]) }}">{{ str_replace('_',' ',ucfirst($filter)) }}</a>@endforeach</div>
<div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Type</th><th>Title</th><th>Status</th><th>Visibility</th><th>Date</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td><span class="tag">{{ $item->type_label }}</span></td><td><strong>{{ $item->title }}</strong><small>{{ $item->reference_code }}</small></td><td>{{ ucfirst($item->status) }}</td><td>{{ $item->is_public ? 'Public' : 'Hidden' }}</td><td>{{ optional($item->published_at)->format('d M Y') ?: '—' }}</td><td class="table-actions"><a href="{{ route('admin.content.edit',$item) }}">Edit</a><form method="post" action="{{ route('admin.content.destroy',$item) }}" onsubmit="return confirm('Delete this content item?')">@csrf @method('DELETE')<button type="submit">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="empty-cell">No content items yet.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $items->links() }}</div>
@endsection
